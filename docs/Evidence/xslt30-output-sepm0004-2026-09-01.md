# XSLT30 document-shaped serialization requirements -- 2026-09-01

## Result

FastXSLT executes the unchanged W3C XSLT30 cases `output-0182` and
`output-0183` and reports native serialization error `SEPM0004`.

Both stylesheets construct three top-level result elements:

- `output-0182` requests `standalone="yes"`;
- `output-0183` requests an external document type through `doctype-system`.

Each property requires a document-shaped result with exactly one top-level
element. FastXSLT validates the semantic result before emitting declaration,
DOCTYPE, or result bytes and returns a structured invalid outcome carrying the
request identity.

The validation applies to both bounded string and byte serialization entry
points. Fragment serialization remains available when neither standalone nor
an external document type imposes the document-element requirement.

## Denominator movement

The complete output denominator moves from 74 to 76 passes and from 158 to 156
visible default not-run cases. Across the eleven conserved XSLT30 denominators,
the total moves from 272 to 274 passes, with 3 engine unsupported cases, 50
profile exclusions, and 204 visible default not-run cases.
