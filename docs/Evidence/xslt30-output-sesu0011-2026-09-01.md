# XSLT30 unsupported normalization form -- 2026-09-01

## Result

FastXSLT executes the unchanged W3C XSLT30 cases `output-0189`,
`output-0190`, `output-0192`, and `output-0193`. It reports native
serialization error `SESU0011` for the unavailable normalization form `ABC`
across XML, XHTML, and text output. The `fully-normalized` request in
`output-0193` admits the same outcome through its native `SERE0012` or
`SESU0011` alternative.

The compiler retains the requested normalization form as immutable
serialization metadata. The bounded serializer reports a structured
unsupported outcome carrying the request identity only when output
materialization requires a normalization capability FastXSLT does not provide.
Both string and byte entry points perform the same capability check before
emitting result content.

## Boundary conservation

This tranche does not admit Unicode normalization. `normalization-form="none"`
continues to preserve result characters exactly; every other requested form
currently reports `SESU0011`. The adjacent HTML case `output-0191` remains
visibly not run because the HTML output method is independently outside the
admitted serializer, and this work does not use normalization failure to
conceal that broader capability gap.

No platform normalization API, ambient locale, filesystem, or host callback is
consulted.

## Denominator movement

The complete output denominator moves from 77 to 81 passes and from 155 to 151
visible default not-run cases. Across the eleven conserved XSLT30 denominators,
the total moves from 275 to 279 passes, with 3 engine unsupported cases, 50
profile exclusions, and 199 visible default not-run cases.
