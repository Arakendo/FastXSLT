# XSLT30 inconsistent serialization parameters -- 2026-08-31

## Result

FastXSLT executes the unchanged W3C XSLT30 cases `output-0186` and
`output-0187` and reports the native serialization error `SEPM0009`.

- `output-0186` combines `omit-xml-declaration="yes"` with
  `standalone="yes"`, whose requested declaration property cannot be emitted.
- `output-0187` combines an omitted declaration, serialization version `1.1`,
  and an external document type, an internally inconsistent XML parameter set.

The checks occur at both bounded string and byte serialization entry points,
before result bytes are emitted. The failure remains a structured invalid
operation associated with the request identity.

## Boundary conservation

General XML 1.1 serialization remains outside the private serializer. The
compiler retains a non-1.0 version only when the other statically retained
parameters guarantee `SEPM0009` before XML 1.1 serialization can occur;
ordinary version `1.1` declarations still report the existing explicit
unsupported boundary.

The byte lane validates the caller's original settings before constructing its
internal declaration-free body settings, so its implementation mechanics do
not manufacture a false `SEPM0009` for otherwise valid standalone or document
type requests.

## Denominator movement

The complete output denominator moves from 72 to 74 passes and from 160 to 158
visible default not-run cases. Across the eleven conserved XSLT30 denominators,
the total moves from 270 to 272 passes, with 3 engine unsupported cases, 50
profile exclusions, and 206 visible default not-run cases.
