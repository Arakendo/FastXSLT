# XSLT30 XML 1.0 namespace undeclaration -- 2026-09-01

## Result

FastXSLT executes the unchanged W3C XSLT30 case `output-0188` and reports
native serialization error `SEPM0010` when XML output version 1.0 requests
`undeclare-prefixes="yes"`.

The stylesheet compiler parses the property using the applicable XSLT 2.0
boolean lexical rules and retains it as immutable serialization metadata. Both
bounded serialization entry points validate the resulting parameter
combination before emitting declaration or result bytes and preserve the
request identity in the structured invalid outcome.

## Boundary conservation

This case does not admit XML 1.1 serialization or general namespace
undeclaration output. `undeclare-prefixes="no"`, an absent property, and output
methods not governed by this XML 1.0 inconsistency do not manufacture
`SEPM0010`. Namespace construction and fixup remain semantic responsibilities
separate from this serialization-parameter check.

## Denominator movement

The complete output denominator moves from 81 to 82 passes and from 151 to 150
visible default not-run cases. Across the eleven conserved XSLT30 denominators,
the total moves from 279 to 280 passes, with 3 engine unsupported cases, 50
profile exclusions, and 198 visible default not-run cases.
