suite = "W3C XSLT 3.0 test suite"
revision = "6f8fd9e966ae74a251a2604abef9d904c7bc5c9b"
set_file = "tests/decl/strip-space/_strip-space-test-set.xml"
case_count = 30

[default_disposition]
selection = "harness-unsupported"
execution = "not-run"
rationale = "Unless overridden below, the whitespace case has not been admitted through the exact strip-all profile. Name tests, preserve-space precedence, schema-aware behavior, xml:space interaction, temporary trees, and broader assertion semantics remain visible rather than being guessed as engine failures."

[[case_override]]
case_name = "strip-space-012"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 1.0+ case applies xsl:strip-space elements='*' to one inline source through the immutable invocation visibility view and matches the native XML assertion."
