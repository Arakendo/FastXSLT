suite = "W3C XSLT 3.0 test suite"
revision = "6f8fd9e966ae74a251a2604abef9d904c7bc5c9b"
set_file = "tests/misc/built-in-templates/_built-in-templates-test-set.xml"
case_count = 6

[default_disposition]
selection = "harness-unsupported"
execution = "not-run"
rationale = "Unless overridden below, the built-in-template case has not been admitted through the private execution slice. Tunnel-like parameter propagation, sequence-constructor typing, and schema annotation remain visible rather than being guessed as engine failures."

[[case_override]]
case_name = "built-in-templates-0101"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 2.0 case carries #current through recursive built-in document and element rules and matches its native XML assertion."

[[case_override]]
case_name = "built-in-templates-0102"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 2.0 case resolves #default to the unnamed mode and carries it through recursive built-in document and element rules, matching its native XML assertion."
