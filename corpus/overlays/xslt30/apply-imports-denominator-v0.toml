suite = "W3C XSLT 3.0 test suite"
revision = "6f8fd9e966ae74a251a2604abef9d904c7bc5c9b"
set_file = "tests/insn/apply-imports/_apply-imports-test-set.xml"
case_count = 1

[default_disposition]
selection = "harness-unsupported"
execution = "not-run"
rationale = "The sole native case applies templates and xsl:apply-imports to atomic integer values across three modules. FastXSLT's admitted apply-imports path currently owns source and temporary-tree node focus only; the atomic focus remains visible rather than being approximated or reported as an engine failure."
