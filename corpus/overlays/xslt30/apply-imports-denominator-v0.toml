suite = "W3C XSLT 3.0 test suite"
revision = "6f8fd9e966ae74a251a2604abef9d904c7bc5c9b"
set_file = "tests/insn/apply-imports/_apply-imports-test-set.xml"
case_count = 1

[default_disposition]
selection = "harness-unsupported"
execution = "not-run"
rationale = "Unless overridden below, an apply-imports case remains visible until its focus kind, import ancestry, built-in fallback, and native assertion are all owned by the executable harness."

[[case_override]]
case_name = "apply-imports-001"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 3.0 case dispatches the integer sequence 1 to 5 through atomic threshold patterns and lower import-precedence continuations, preserving built-in atomic string-value behavior."
