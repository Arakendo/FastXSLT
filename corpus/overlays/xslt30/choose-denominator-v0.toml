suite = "W3C XSLT 3.0 test suite"
revision = "6f8fd9e966ae74a251a2604abef9d904c7bc5c9b"
set_file = "tests/insn/choose/_choose-test-set.xml"
case_count = 55

[default_disposition]
selection = "harness-unsupported"
execution = "not-run"
rationale = "Unless overridden below, the choose/if case depends on expression, type, namespace, collation, import, function, initial-template, schema, or assertion semantics outside the currently admitted private execution slice."

[[case_override]]
case_name = "choose-0101"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 1.0-compatible case performs ordered child string-value comparisons inside source-node iteration and takes the first matching branch or xsl:otherwise."

[[case_override]]
case_name = "choose-0102"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 1.0-compatible case composes exact strip-all whitespace visibility with child string-value branch tests and empty choose branches."

[[case_override]]
case_name = "choose-0201"
selection = "selected"
execution = "passed"
rationale = "The unchanged case iterates critter elements and emits an empty result when neither attribute-value branch matches and no xsl:otherwise exists."

[[case_override]]
case_name = "choose-0301"
selection = "selected"
execution = "passed"
rationale = "The unchanged case treats comparisons against a missing attribute as false for every iterated critter and emits no branch content."

[[case_override]]
case_name = "choose-0401"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 1.0-compatible case takes a single true xsl:when branch selected by child existence and matches the native XML assertion."

[[case_override]]
case_name = "choose-0402"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 1.0-compatible case skips a false branch, takes the following true branch, and matches the native XML assertion."

[[case_override]]
case_name = "choose-0403"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 1.0-compatible case takes xsl:otherwise after a false child-existence test and matches the native XML assertion."

[[case_override]]
case_name = "choose-0404"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 1.0-compatible case takes the true xsl:when branch and proves the following xsl:otherwise branch is not evaluated."

[[case_override]]
case_name = "choose-0501"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 1.0-compatible case produces an empty sequence when every xsl:when is false and no xsl:otherwise is present."

[[case_override]]
case_name = "choose-0502"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 1.0-compatible case produces an empty sequence for one false xsl:when with no xsl:otherwise."

[[case_override]]
case_name = "choose-0601"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 1.0-compatible case executes two true xsl:if instructions using numeric and string equality and matches the native XML assertion."

[[case_override]]
case_name = "choose-0602"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 1.0-compatible case applies effective boolean value to non-empty string and zero numeric literals and matches the native XML assertion."

[[case_override]]
case_name = "choose-0604"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 2.0 case preserves whitespace-only stylesheet text inherited from xml:space='preserve' on xsl:choose while executing a nested true xsl:if."

[[case_override]]
case_name = "choose-0605"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 1.0-compatible case uses the already admitted exact round() constant evaluator in an xsl:when comparison."

[[case_override]]
case_name = "choose-0606"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 2.0 case applies effective boolean value to non-empty xs:untypedAtomic and empty xs:string global variables."

[[case_override]]
case_name = "choose-0609"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 2.0 case preserves an empty-sequence global distinctly from an empty xs:untypedAtomic value and proves all exact empty tests emit no result content."

[[case_override]]
case_name = "choose-0701"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 1.0-compatible case compares each selected context node's string value to a literal and emits content only for matching nodes."

[[case_override]]
case_name = "choose-0702"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 1.0-compatible case negates a context string-value comparison and preserves selected-node order and focus."

[[case_override]]
case_name = "choose-0801"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 1.0-compatible case filters a relative child path by context string equality and emits values from the matching parent context."

[[case_override]]
case_name = "choose-0901"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 1.0-compatible case short-circuits a two-operand or expression over attribute string comparisons while iterating source children."

[[case_override]]
case_name = "choose-1001"
selection = "selected"
execution = "passed"
rationale = "The unchanged case compares one child node's untyped string value to successive integer bounds and instantiates only the first true xsl:when branch."

[[case_override]]
case_name = "choose-1101"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 1.0-compatible case nests seven xsl:if instructions over exact constant arithmetic, context string, and unqualified parent-name comparisons."

[[case_override]]
case_name = "choose-1201"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 2.0 case nests ordered choices while walking context-relative title descendants and looking ahead through positional following-sibling paths."

[[case_override]]
case_name = "choose-1202"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 1.0-compatible case iterates context-relative descendants and selects the first matching unqualified name branch across a 26-way xsl:choose."

[[case_override]]
case_name = "choose-1203"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 1.0-compatible case iterates context-relative descendants and selects the first of 26 exact context string-length branches."

[[case_override]]
case_name = "choose-1301"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 2.0 case binds the current source-template focus position to a local variable and selects the matching ordered branch."

[[case_override]]
case_name = "choose-1401"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 1.0-compatible case skips a false numeric xsl:when, enters xsl:otherwise, and executes a nested xsl:if whose exact modulo comparison is true."

[[case_override]]
case_name = "choose-1501"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 2.0 case applies inherited and instruction-local xpath-default-namespace values while counting source descendants under true typed-boolean conditionals."

[[case_override]]
case_name = "choose-1502"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 2.0 case applies empty and reset xpath-default-namespace values across xsl:choose, xsl:when, xsl:otherwise, and descendant count expressions."

[[case_override]]
case_name = "choose-1601"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 2.0 case applies numeric effective-boolean-value rules to NaN, zero, and a nonzero xs:double across three xsl:if instructions."

[[case_override]]
case_name = "choose-1703"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 2.0 case selects an xsl:when through the effective boolean value of a non-empty xs:untypedAtomic global variable."

[[case_override]]
case_name = "choose-1704"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 2.0 case selects xsl:otherwise through the false effective boolean value of an empty xs:string global variable."

[[case_override]]
case_name = "choose-1706"
selection = "selected"
execution = "passed"
rationale = "The unchanged XSLT 2.0 case applies effective-boolean-value rules to an empty source-dependent numeric sequence, zero, and a nonzero xs:double across ordered choices."

[[case_override]]
case_name = "choose-1801"
selection = "selected"
execution = "passed"
rationale = "The unchanged negative case reports standard static error XTSE0010 when xsl:if omits its required test attribute."

[[case_override]]
case_name = "choose-1802"
selection = "selected"
execution = "passed"
rationale = "The unchanged negative case reports standard static error XTSE0010 when xsl:when omits its required test attribute."

[[case_override]]
case_name = "choose-1803"
selection = "selected"
execution = "passed"
rationale = "The unchanged negative case reports standard static error XTSE0010 when xsl:when follows xsl:otherwise."

[[case_override]]
case_name = "choose-1804"
selection = "selected"
execution = "passed"
rationale = "The unchanged negative case reports standard static error XTSE0010 for a second xsl:otherwise child."
